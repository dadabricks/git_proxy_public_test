[project]
name = "databricks-repo-proxy"
version = "0.0.13"
dependencies = []

[project.scripts]
db_proxy = "proxy.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"